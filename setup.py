from setuptools import setup, find_packages
setup(
    name='pytop',
    version='0.0.1',
    description='A small library for parsing monitor commands (top, nvidia-smi)',
    url='https://github.com/justinjfu/pytop',
    license='MIT',
    packages=find_packages(),
)
